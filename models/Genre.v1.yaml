title: Genre
type: object
properties:
  guid:
    type: string
  _links:
    $ref: ./HalLinks.v1.yaml
  names:
    type: array
    items:
      type: object
      properties:
        metadata:
          $ref: ./metadata_field.v1.yaml
        name:
          type: string
  updated_at:
    type: string
    format: date-time
  created_at:
    type: string
    format: date-time
required:
  - guid
  - _links
  - names
  - updated_at
